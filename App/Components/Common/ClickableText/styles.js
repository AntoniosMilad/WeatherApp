import {StyleSheet} from 'react-native';
import {Colors, Fonts} from '../../../Themes';

export default StyleSheet.create({
  text: {
    ...Fonts.style.smallBold,
    color: Colors.blueBlueTwo,
  },
});
